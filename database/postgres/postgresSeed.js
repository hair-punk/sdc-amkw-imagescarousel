const writeCSV = require('../datageneration');
const dbsetup = require('./dbsetup');
const writedb = require('./writedb');

const seed = () => {
//   for (let j = 0; j < 1; j++) {
//     writeCSV(2);
//     // loadFromCSV(table);
//   }
};

seed();